def f = [X | Y] link X >-> Y

def main = <a |> [|]
    use f [X | Y] as F1
    use -a [X | Y] as RemA
    use f [X | Y] as F2
    link
        F1.Y >-> RemA.X
        RemA.Y >-> F2.X
        F2.Y >-> F1.X
