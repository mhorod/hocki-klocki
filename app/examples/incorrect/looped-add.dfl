def forward = [X | Y]
    link
        X >-> Y

def loop = <a |> [|]
    use forward [X | Y] as F1
    use +a [X | Y] as AddA
    use forward [X | Y] as F2
    link
        F1.Y >-> AddA.X
        AddA.Y >-> F2.X
        F2.Y >-> F1.X
